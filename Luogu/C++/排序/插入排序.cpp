#include <iostream>
int* Insert_Sort(int a[]) {
	int over = -1;
	int len = sizeof(a) / sizeof(a[0]);
	int compare;
	int temp;
	for (;over < len;++over) {
		int n = len;
		compare = over;
		if(a[compare]<a[n])


	}
}

int main() {

}